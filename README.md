
  # Kcalories Monitor App

  This is a code bundle for Kcalories Monitor App. The original project is available at https://www.figma.com/design/Liarkp574ZACmORGSFSKaM/Kcalories-Monitor-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  